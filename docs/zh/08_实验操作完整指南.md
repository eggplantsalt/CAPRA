# 08 实验操作完整指南（傻瓜教程）

> **目标读者：** 从未接触过本项目的研究者，从一台干净的 Linux 服务器出发完整跑通所有实验。
>
> **重要约定：** 所有命令在服务器终端执行；路径均相对项目根目录；每节末「验证」步骤必做；出错查 `docs/zh/06_故障排查.md`。

---

## 目录

1. [实验全景与时间估计](#1-实验全景与时间估计)
2. [服务器硬件要求确认](#2-服务器硬件要求确认)
3. [第一步：克隆代码仓库](#3-第一步克隆代码仓库)
4. [第二步：配置 Conda 环境](#4-第二步配置-conda-环境)
5. [第三步：安装所有依赖](#5-第三步安装所有依赖)
6. [第四步：冒烟测试](#6-第四步冒烟测试验证安装)
7. [第五步：下载预训练模型](#7-第五步下载预训练模型)
8. [第六步：下载 LIBERO 数据集](#8-第六步下载-libero-数据集)
9. [第七步：验证数据集可加载](#9-第七步验证数据集可加载)
10. [实验 A：Baseline 评估](#10-实验a-baseline-评估)
11. [实验 B：Baseline 训练](#11-实验b-baseline-训练)
12. [实验 C：CAPRA 离线挖掘](#12-实验c-capra-离线挖掘)
13. [实验 D：CAPRA 训练](#13-实验d-capra-训练)
14. [实验 E：CAPRA 模型评估](#14-实验e-capra-模型评估)
15. [实验 F：SafeLIBERO 评估](#15-实验f-safelibero-评估)
16. [实验 G：程序化场景评估](#16-实验g-程序化场景评估)
17. [实验 H：完整 CF 评估](#17-实验h-完整-cf-评估spirear)
18. [多任务套件批量实验](#18-多任务套件批量实验)
19. [查看与解读结果](#19-查看与解读结果)
20. [关键配置参数速查表](#20-关键配置参数速查表)
21. [常见错误与解决方案](#21-常见错误与解决方案)

---

## 1. 实验全景与时间估计

```
[环境搭建] → [下载模型+数据集] → [实验A: Baseline评估]
                                         ↓
                               [实验B: Baseline训练（对照组）]
                                         ↓
                               [实验C: CAPRA离线挖掘]  ← 需要：模型+LIBERO环境
                                         ↓
                               [实验D: CAPRA训练]      ← 需要：缓存+数据集
                                         ↓
                               [实验E: CAPRA评估]      ← 需要：CAPRA检查点
                                         ↓
           ┌──────────────────────────────────────────────────┐
           │实验F: SafeLIBERO │ 实验G: 程序化场景 │ 实验H: CF │
           └──────────────────────────────────────────────────┘
```

| 阶段 | 操作 | 预计时间（A100 80GB 单卡） |
|---|---|---|
| 环境搭建 | Conda + 依赖安装 | 30-60 分钟 |
| 模型下载 | OpenVLA-OFT 检查点 (~14 GB/个) | 20-40 分钟 |
| 数据集下载 | 全部 LIBERO (~10 GB) | 30-60 分钟 |
| 实验 A | Baseline 评估（500 episodes） | 2-4 小时 |
| 实验 B | Baseline 训练（200K 步） | 24-48 小时 |
| 实验 C | CAPRA 挖掘（50 episodes） | 2-6 小时 |
| 实验 D | CAPRA 训练（200K 步） | 24-48 小时 |
| 实验 E-H | 各约 2-4 小时 | 2-4 小时/个 |

> **快速通路验证：** 每步加 `--num_trials_per_task 3`、`--num_mining_episodes 1`、`--max_steps 1000`，几小时内跑完全部流程。

---

## 2. 服务器硬件要求确认

```bash
nvidia-smi        # GPU：评估 >=16GB VRAM，训练 >=27GB VRAM
free -h           # CPU 内存：至少 64 GB
df -h ~           # 磁盘：至少 100 GB 可用
nvcc --version    # CUDA：推荐 11.8 或 12.1
conda --version   # 需要 conda 已安装
```

`nvidia-smi` 报错则联系服务器管理员安装 GPU 驱动。

---

## 3. 第一步：克隆代码仓库

```bash
cd /path/to/your/workspace
git clone https://github.com/your-org/openvla-oft.git
cd openvla-oft

export PROJECT_ROOT=$(pwd)
echo "export PROJECT_ROOT=$PROJECT_ROOT" >> ~/.bashrc
```

**以下所有操作均在 `$PROJECT_ROOT` 目录下执行。**

### 验证

```bash
ls $PROJECT_ROOT/experiments/robot/capra/
# 应看到：core/  mining/  eval/  scene/ 等

ls $PROJECT_ROOT/scripts/capra/
# 应看到：mine_capra.sh  train_capra.sh  eval_capra.sh  smoke_capra.sh
```

---

## 4. 第二步：配置 Conda 环境

```bash
conda create -n openvla python=3.10 -y
conda activate openvla

# CUDA 11.8
pip install torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 \
    --index-url https://download.pytorch.org/whl/cu118

# CUDA 12.1（按需选择）
# pip install torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 \
#     --index-url https://download.pytorch.org/whl/cu121
```

### 验证

```bash
python -c "import torch; print('PyTorch:', torch.__version__, '| CUDA:', torch.cuda.is_available())"
# 必须看到 CUDA: True
```

---

## 5. 第三步：安装所有依赖

```bash
conda activate openvla
cd $PROJECT_ROOT

# 5.1 安装项目本身
pip install -e .

# 5.2 Flash Attention 2（约 10-15 分钟）
pip install packaging ninja
ninja --version && echo "ninja OK"
pip install "flash-attn==2.5.5" --no-build-isolation

# 5.3 LIBERO 仿真环境
pip install libero
pip install -r experiments/robot/libero/libero_requirements.txt

# 5.4 训练/日志/数据工具
pip install wandb accelerate peft
pip install tensorflow tensorflow-datasets
pip install draccus imageio tqdm
```

### 验证

```bash
conda activate openvla
python -c "
import torch, libero, tensorflow, draccus, peft, wandb
from experiments.robot.capra.core.capra_config import CAPRAConfig
print('所有核心包导入成功！')
"
```

---

## 6. 第四步：冒烟测试（验证安装）

**不需要 GPU、不需要模型。** 进行任何实验前必须先跑这一步。

```bash
conda activate openvla
cd $PROJECT_ROOT
bash scripts/capra/smoke_capra.sh
```

**预期输出：**

```
[smoke] [1/11]  CAPRAConfig + FinetuneCAPRAConfig ... OK
[smoke] [2/11]  Equivalence filter ... OK
[smoke] [3/11]  q_hat_t distribution ... OK
[smoke] [4/11]  CAPRA KL loss (CPU tensor) ... OK
[smoke] [5/11]  Training branches ... OK
[smoke] [6/11]  SPIR / EAR metrics ... OK
[smoke] [7/11]  Episode + Aggregate metrics ... OK
[smoke] [8/11]  Report generation ... OK
[smoke] [9/11]  Precursor attribution w_t ... OK
[smoke] [10/11] Procedural templates ... OK
[smoke] [11/11] EnvAdapter no-sim path ... OK
[smoke_capra] 所有检查通过 / All checks passed.
```

失败时运行 `python scripts/capra/_smoke_logic.py` 查看详细报错。

---

## 7. 第五步：下载预训练模型

```bash
conda activate openvla && cd $PROJECT_ROOT
mkdir -p tmp/models
huggingface-cli login
```

### 7.1 任务专用评估检查点（实验 A 和 C 使用）

```bash
huggingface-cli download moojink/openvla-7b-oft-finetuned-libero-spatial \
    --local-dir tmp/models/openvla-7b-oft-finetuned-libero-spatial
huggingface-cli download moojink/openvla-7b-oft-finetuned-libero-object \
    --local-dir tmp/models/openvla-7b-oft-finetuned-libero-object
huggingface-cli download moojink/openvla-7b-oft-finetuned-libero-goal \
    --local-dir tmp/models/openvla-7b-oft-finetuned-libero-goal
huggingface-cli download moojink/openvla-7b-oft-finetuned-libero-10 \
    --local-dir tmp/models/openvla-7b-oft-finetuned-libero-10
```

### 7.2 基础 OpenVLA-7B 模型（实验 B 和 D 训练用）

```bash
huggingface-cli download openvla/openvla-7b --local-dir tmp/models/openvla-7b
```

### 验证

```bash
ls tmp/models/openvla-7b-oft-finetuned-libero-spatial/
python -c "
from transformers import AutoConfig
cfg = AutoConfig.from_pretrained(
    'tmp/models/openvla-7b-oft-finetuned-libero-spatial', trust_remote_code=True)
print('模型加载成功:', cfg.model_type)
"
```

---

## 8. 第六步：下载 LIBERO 数据集

**仅实验 B 和 D（训练）需要。** 只做评估实验可跳过。

```bash
conda activate openvla && cd $PROJECT_ROOT
mkdir -p tmp/datasets/rlds
huggingface-cli download openvla/modified_libero_rlds \
    --local-dir tmp/datasets/rlds_download
mv tmp/datasets/rlds_download/libero_spatial_no_noops  tmp/datasets/rlds/libero_spatial_no_noops
mv tmp/datasets/rlds_download/libero_object_no_noops   tmp/datasets/rlds/libero_object_no_noops
mv tmp/datasets/rlds_download/libero_goal_no_noops     tmp/datasets/rlds/libero_goal_no_noops
mv tmp/datasets/rlds_download/libero_10_no_noops       tmp/datasets/rlds/libero_10_no_noops
```

`--data_root_dir` 指向 `tmp/datasets/rlds/`；`--dataset_name` 与子目录名完全一致（如 `libero_spatial_no_noops`）。

### 验证

```bash
ls tmp/datasets/rlds/libero_spatial_no_noops/
# 应看到 dataset_info.json 和 1.0.0/ 目录
```

---

## 9. 第七步：验证数据集可加载

```bash
conda activate openvla && cd $PROJECT_ROOT
python - <<'EOF'
import tensorflow_datasets as tfds
builder = tfds.builder_from_directory('tmp/datasets/rlds/libero_spatial_no_noops')
ds = builder.as_dataset(split='train', shuffle_files=False)
for ep in ds.take(1):
    s = list(ep['steps'].as_numpy_iterator())
    print('Episode 步数:', len(s))
    print('动作维度:', s[0]['action'].shape)               # (7,)
    print('图像维度:', s[0]['observation']['image'].shape) # (256,256,3)
    instr = s[0]['language_instruction']
    print('语言指令:', instr.decode() if isinstance(instr,bytes) else instr)
    print('RLDS 验证通过！')
EOF
```

---

## 10. 实验 A：Baseline 评估

**目的：** 评估原版 OpenVLA-OFT 检查点的任务成功率，作为 CAPRA 对照基线。
**前提：** 第 7 步模型已下载。

```bash
conda activate openvla && cd $PROJECT_ROOT

# 方式一：原版脚本（最接近论文结果）
python experiments/robot/libero/run_libero_eval.py \
    --pretrained_checkpoint tmp/models/openvla-7b-oft-finetuned-libero-spatial \
    --task_suite_name libero_spatial --center_crop True

# 方式二：CAPRA 评估脚本（推荐，输出更完整）
# 参数顺序：检查点 / 套件名 / 每任务次数 / CF候选数(0=obs-only) / 模板名(留空)
bash scripts/capra/eval_capra.sh \
    tmp/models/openvla-7b-oft-finetuned-libero-spatial libero_spatial 50
```

### 评估全部 4 个套件

```bash
for SUITE in libero_spatial libero_object libero_goal libero_10; do
    bash scripts/capra/eval_capra.sh \
        tmp/models/openvla-7b-oft-finetuned-libero-${SUITE} ${SUITE} 50
done
```

### 查看结果

结果保存在 `experiments/logs/capra/CAPRA-EVAL-{suite}-{datetime}/`。

```bash
python -c "
import json, glob
f = sorted(glob.glob('experiments/logs/capra/CAPRA-EVAL-libero_spatial-*/results_aggregate.json'))
if f:
    d = json.load(open(f[-1]))
    print('success_rate:', round(d['success_rate'],3))  # 论文约 0.96-0.97
    print('topple_rate: ', d.get('topple_rate','N/A'))
"
```

`spir_mean`/`ear_mean` 此时为 0 是正确空值（obs-only 模式不计算这两个指标）。

---

## 11. 实验 B：Baseline 训练

**目的：** 用 L1 回归微调 OpenVLA-7B 作为公平对照组。只评估预训练检查点可**跳过**。

**前提：** `tmp/models/openvla-7b` 和 `tmp/datasets/rlds/libero_spatial_no_noops` 已准备好。

```bash
conda activate openvla && cd $PROJECT_ROOT

torchrun --standalone --nnodes 1 --nproc-per-node 1 \
    vla-scripts/finetune_capra.py \
    --vla_path tmp/models/openvla-7b \
    --dataset_name libero_spatial_no_noops \
    --data_root_dir tmp/datasets/rlds \
    --run_root_dir runs \
    --capra_enabled False \
    --use_l1_regression True --use_proprio True \
    --num_images_in_input 2 \
    --use_lora True --lora_rank 32 \
    --batch_size 8 --shuffle_buffer_size 2000 \
    --learning_rate 5e-4 \
    --num_steps_before_decay 100000 --max_steps 200000 \
    --save_freq 10000 --image_aug True
# 多卡：把 --nproc-per-node 1 改为 GPU 数量（如 4）
```

L1 loss 降至 0.01 以下为正常（约 150K 步）。LIBERO-Goal 例外，50K 步通常更好。

---

## 12. 实验 C：CAPRA 离线挖掘

**目的：** 用基线模型在仿真中运行，对每步采样 K 个候选动作，通过短时反事实 rollout 计算 P_t/F_t，生成安全监督信号缓存。

**前提：** 第 7 步模型已下载；LIBERO 已安装；服务器支持 MuJoCo 渲染。

### 设置 MuJoCo 离屏渲染

```bash
export MUJOCO_GL=osmesa
export PYOPENGL_PLATFORM=osmesa
echo 'export MUJOCO_GL=osmesa' >> ~/.bashrc
echo 'export PYOPENGL_PLATFORM=osmesa' >> ~/.bashrc
# 安装 osmesa（需 root）：sudo apt-get install -y libosmesa6-dev
# 无 root 时尝试：export MUJOCO_GL=egl
```

### 运行挖掘

```bash
conda activate openvla && cd $PROJECT_ROOT

# 一键脚本（推荐）
bash scripts/capra/mine_capra.sh \
    tmp/models/openvla-7b-oft-finetuned-libero-spatial \
    libero_spatial tmp/capra_cache

# 完整 CLI
python -m experiments.robot.capra.mining.run_capra_mining \
    --pretrained_checkpoint tmp/models/openvla-7b-oft-finetuned-libero-spatial \
    --dataset_name libero_spatial --cache_root tmp/capra_cache \
    --num_mining_episodes 50 --K 8 --H_s 5 --W 10 \
    --progress_floor 0.20 --epsilon_p_abs 0.05 --seed 7

# 快速验证（1 个 episode，约 10-30 分钟）
python -m experiments.robot.capra.mining.run_capra_mining \
    --pretrained_checkpoint tmp/models/openvla-7b-oft-finetuned-libero-spatial \
    --dataset_name libero_spatial --cache_root tmp/capra_cache \
    --num_mining_episodes 1
```

| 参数 | 默认值 | 调整建议 |
|---|---|---|
| `--K` | 8 | 减小(4)加速；增大(16)提升质量 |
| `--H_s` | 5 | 减小(3)加速；增大(8)提升精度 |
| `--num_mining_episodes` | 50 | 先用 1 验证流程 |
| `--progress_floor` | 0.20 | 激活率为 0 时降至 0.10 |

**断点续传：** 中途中断后直接重新运行，已完成的 episode 自动跳过。

### 验证

```bash
ls tmp/capra_cache/libero_spatial/ | wc -l
# 应等于 num_mining_episodes（默认 50）
```